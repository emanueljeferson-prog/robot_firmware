[package]
name = "firmware"
version = "0.1.0"
edition = "2024"

[dependencies]
spin = "0.12.0"
serde-json-core = "0.6.0"
serde = { version = "1.0", default-features = false, features = ["alloc", "derive"] }